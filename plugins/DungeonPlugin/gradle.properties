# ============================================================
#  Version settings - mirrors DungeonForge's gradle.properties
# ============================================================

# The paper-api version compiled against. See DungeonForge's
# gradle.properties for how to find current build numbers.
paperApiVersion=26.1.2.build.74-stable

# Must match the Minecraft version above. Ends up in plugin.yml.
apiVersion=26.1.2

pluginGroup=nl.riddernix
pluginVersion=0.1.0

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true
